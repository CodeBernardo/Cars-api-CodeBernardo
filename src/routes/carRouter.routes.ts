import { Router } from "express";

const carRouter = Router();

carRouter.post("",);

carRouter.get("",);

carRouter.get("/:id",);

carRouter.patch("/:id",);

carRouter.delete("/:id",);

export default carRouter;
